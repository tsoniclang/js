{
  "name": "tsonic-js-workspace",
  "private": true,
  "description": "Repo for @tsonic/js (versioned by .NET major in versions/<major>/)",
  "type": "module",
  "scripts": {
    "generate:10": "echo '@tsonic/js is source-of-truth TypeScript; no generation step'",
    "selftest": "bash scripts/selftest.sh",
    "publish:10": "bash scripts/selftest.sh && npm publish ./versions/10 --access public",
    "build": "tsonic build",
    "dev": "tsonic run"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {}
}
