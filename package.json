{
  "name": "tsonic-js-workspace",
  "private": true,
  "description": "Repo for @tsonic/js portable source surface",
  "type": "module",
  "scripts": {
    "generate:10": "echo '@tsonic/js is source-authored; no generation step'",
    "selftest": "node --check versions/10/runtime/index.js",
    "publish:10": "npm run selftest && npm publish ./versions/10 --access public"
  },
  "devDependencies": {}
}
